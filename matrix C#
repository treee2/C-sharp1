int[,] array3 = { 
   {2, 2, 3 }, 
   {2, 2, 2 },
   {5, 6, 7 },
};
for (int i = 0; i < array3.GetLength(0); i++)
{
    for (int j = 0; j < array3.GetLength(1); j++)    // это двухмерный массив
    {
        Console.Write(array3[i, j] + " "); //конкатенируем
    }
    Console.WriteLine();
}
